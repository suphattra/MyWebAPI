'use strict';

var Td_languate = require('mongoose').model('td_languate');

exports.getLanguate =  function(req,res){
    console.log('send success');
   // let listLanguate = [];
   /*   Td_languate.find('',(err,reponce)=>{
        console.log(reponce);
        res.json(reponce);
    });  */
};